LATEX_FORMAT = """**【LaTeX格式规范】**：
长公式使用 `$$...$$` 包裹并单独成行；行内公式使用 `$...$` 包裹。
- **矩阵公式**：必须使用单行格式，元素间用 `&` 分隔，行间用 `\\`，整体放在一对 `$$` 内
  - 正确：`$$\begin{pmatrix} a & b \\ c & d \end{pmatrix}$$`
- **分数**：`$\frac{a}{b}$`
- **积分**：`$\int_a^b f(x) dx$`
- **求和**：`$\sum_{i=1}^n a_i$`
- **极限**：`$\lim_{x \to \infty} f(x)$`
- **上下标**：`$x_1$`, `$x^2$`, `$x_i^2$`
- **根号**：`$\sqrt{x}$`, `$\sqrt[n]{x}$`
- **向量**：`$\vec{a}$` 或 `$\mathbf{a}$`
- **希腊字母**：`$\alpha$`, `$\beta$`, `$\theta$` 等"""
